# 自动化发布小红书实现过程

## 打通docker和github

### 通过cloudflare Tunnel持久化部署

#### 在 Cloudflare 控制台创建隧道

1. 登录 [Cloudflare Zero Trust 控制台](https://one.dash.cloudflare.com/)。

2. 导航到 **Networks** -> **Tunnels**。

3. 点击 **Create a Tunnel**，选择 **Cloudflared**。

4. 给隧道起个名字（例如 `mac-n8n-tunnel`），保存。

5. 在 **Install and run a connector** 页面，你会看到一串 **Token**（很长的一段字符串）。**把它复制下来**，后面要用。


